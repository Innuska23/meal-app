import styled from "styled-components";

const FilterContainer = styled.div`
  margin-bottom: 20px;
`;

const ButtonWrapper = styled.div`
  display: flex;
  gap: 10px;
  flex-wrap: wrap;
`;

export const S = {
  FilterContainer,
  ButtonWrapper,
};
